@model Jorfofbasket.Models.Match

@if (!ViewData.ModelState.IsValid)
{
    <div class="alert alert-danger">
        <ul>
        @foreach (var state in ViewData.ModelState)
        {
            foreach (var error in state.Value.Errors)
            {
                <li>@state.Key : @error.ErrorMessage</li>
            }
        }
        </ul>
    </div>
}


<form asp-action="Create" method="post" class="mx-auto" style="max-width:600px;">
    @Html.AntiForgeryToken()

    <div class="mb-3">
        <label asp-for="EquipeAdverse" class="form-label"></label>
        <input asp-for="EquipeAdverse" class="form-control" />
        <span asp-validation-for="EquipeAdverse" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="DateMatch" class="form-label"></label>
        <!-- <input asp-for="DateMatch" type="date" class="form-control" /> -->
        <input asp-for="DateMatch" type="date" class="form-control" value="@DateTime.Now.ToString("yyyy-MM-dd")" />
        <span asp-validation-for="DateMatch" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="ScoreEquipe" class="form-label"></label>
        <input asp-for="ScoreEquipe" type="number" class="form-control" value="0" />
        <span asp-validation-for="ScoreEquipe" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="ScoreAdverse" class="form-label"></label>
        <input asp-for="ScoreAdverse" type="number" class="form-control" value="0" />
        <span asp-validation-for="ScoreAdverse" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="Lieu" class="form-label"></label>
        <input asp-for="Lieu" class="form-control" />
        <span asp-validation-for="Lieu" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="Statut" class="form-label"></label>
        <select asp-for="Statut" class="form-select">
            <option value="Prévu" selected>Prévu</option>
            <option value="Terminé">Terminé</option>
            <option value="Reporté">Reporté</option>
        </select>
        <span asp-validation-for="Statut" class="text-danger"></span>
    </div>

    <div class="text-center">
        <button type="submit" class="btn btn-success">💾 Enregistrer</button>
        <a asp-action="Index" class="btn btn-secondary ms-2">↩️ Retour</a>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
